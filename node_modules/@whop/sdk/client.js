"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
var _Whop_instances, _a, _Whop_encoder, _Whop_baseURLOverridden;
Object.defineProperty(exports, "__esModule", { value: true });
exports.Whop = void 0;
const tslib_1 = require("./internal/tslib.js");
const uuid_1 = require("./internal/utils/uuid.js");
const values_1 = require("./internal/utils/values.js");
const sleep_1 = require("./internal/utils/sleep.js");
const errors_1 = require("./internal/errors.js");
const detect_platform_1 = require("./internal/detect-platform.js");
const Shims = tslib_1.__importStar(require("./internal/shims.js"));
const Opts = tslib_1.__importStar(require("./internal/request-options.js"));
const query_1 = require("./internal/utils/query.js");
const version_1 = require("./version.js");
const Errors = tslib_1.__importStar(require("./core/error.js"));
const Pagination = tslib_1.__importStar(require("./core/pagination.js"));
const Uploads = tslib_1.__importStar(require("./core/uploads.js"));
const API = tslib_1.__importStar(require("./resources/index.js"));
const api_promise_1 = require("./core/api-promise.js");
const access_tokens_1 = require("./resources/access-tokens.js");
const account_links_1 = require("./resources/account-links.js");
const accounts_1 = require("./resources/accounts.js");
const ad_campaigns_1 = require("./resources/ad-campaigns.js");
const ad_groups_1 = require("./resources/ad-groups.js");
const ad_reports_1 = require("./resources/ad-reports.js");
const ads_1 = require("./resources/ads.js");
const ai_chats_1 = require("./resources/ai-chats.js");
const app_builds_1 = require("./resources/app-builds.js");
const apps_1 = require("./resources/apps.js");
const authorized_users_1 = require("./resources/authorized-users.js");
const bounties_1 = require("./resources/bounties.js");
const chat_channels_1 = require("./resources/chat-channels.js");
const checkout_configurations_1 = require("./resources/checkout-configurations.js");
const companies_1 = require("./resources/companies.js");
const company_token_transactions_1 = require("./resources/company-token-transactions.js");
const conversions_1 = require("./resources/conversions.js");
const course_chapters_1 = require("./resources/course-chapters.js");
const course_lesson_interactions_1 = require("./resources/course-lesson-interactions.js");
const course_lessons_1 = require("./resources/course-lessons.js");
const course_students_1 = require("./resources/course-students.js");
const courses_1 = require("./resources/courses.js");
const deposits_1 = require("./resources/deposits.js");
const dispute_alerts_1 = require("./resources/dispute-alerts.js");
const disputes_1 = require("./resources/disputes.js");
const dm_channels_1 = require("./resources/dm-channels.js");
const dm_members_1 = require("./resources/dm-members.js");
const entries_1 = require("./resources/entries.js");
const experiences_1 = require("./resources/experiences.js");
const fee_markups_1 = require("./resources/fee-markups.js");
const files_1 = require("./resources/files.js");
const forum_posts_1 = require("./resources/forum-posts.js");
const forums_1 = require("./resources/forums.js");
const invoices_1 = require("./resources/invoices.js");
const leads_1 = require("./resources/leads.js");
const ledger_accounts_1 = require("./resources/ledger-accounts.js");
const members_1 = require("./resources/members.js");
const memberships_1 = require("./resources/memberships.js");
const messages_1 = require("./resources/messages.js");
const notifications_1 = require("./resources/notifications.js");
const payment_methods_1 = require("./resources/payment-methods.js");
const payments_1 = require("./resources/payments.js");
const payout_accounts_1 = require("./resources/payout-accounts.js");
const payout_methods_1 = require("./resources/payout-methods.js");
const plans_1 = require("./resources/plans.js");
const products_1 = require("./resources/products.js");
const promo_codes_1 = require("./resources/promo-codes.js");
const reactions_1 = require("./resources/reactions.js");
const refunds_1 = require("./resources/refunds.js");
const resolution_center_cases_1 = require("./resources/resolution-center-cases.js");
const reviews_1 = require("./resources/reviews.js");
const setup_intents_1 = require("./resources/setup-intents.js");
const shipments_1 = require("./resources/shipments.js");
const support_channels_1 = require("./resources/support-channels.js");
const swaps_1 = require("./resources/swaps.js");
const topups_1 = require("./resources/topups.js");
const transfers_1 = require("./resources/transfers.js");
const users_1 = require("./resources/users.js");
const verifications_1 = require("./resources/verifications.js");
const wallets_1 = require("./resources/wallets.js");
const webhooks_1 = require("./resources/webhooks.js");
const withdrawals_1 = require("./resources/withdrawals.js");
const affiliates_1 = require("./resources/affiliates/affiliates.js");
const headers_1 = require("./internal/headers.js");
const env_1 = require("./internal/utils/env.js");
const log_1 = require("./internal/utils/log.js");
const values_2 = require("./internal/utils/values.js");
const verify_user_token_1 = require("./lib/verify-user-token.js");
/**
 * API Client for interfacing with the Whop API.
 */
class Whop {
    /**
     * API Client for interfacing with the Whop API.
     *
     * @param {string | undefined} [opts.apiKey=process.env['WHOP_API_KEY'] ?? undefined]
     * @param {string | null | undefined} [opts.webhookKey=process.env['WHOP_WEBHOOK_SECRET'] ?? null]
     * @param {string | null | undefined} [opts.appID=process.env['WHOP_APP_ID'] ?? null]
     * @param {string} [opts.baseURL=process.env['WHOP_BASE_URL'] ?? https://api.whop.com/api/v1] - Override the default base URL for the API.
     * @param {number} [opts.timeout=1 minute] - The maximum amount of time (in milliseconds) the client will wait for a response before timing out.
     * @param {MergedRequestInit} [opts.fetchOptions] - Additional `RequestInit` options to be passed to `fetch` calls.
     * @param {Fetch} [opts.fetch] - Specify a custom `fetch` function implementation.
     * @param {number} [opts.maxRetries=2] - The maximum number of times the client will retry a request.
     * @param {HeadersLike} opts.defaultHeaders - Default headers to include with every request to the API.
     * @param {Record<string, string | undefined>} opts.defaultQuery - Default query parameters to include with every request to the API.
     */
    constructor({ baseURL = (0, env_1.readEnv)('WHOP_BASE_URL'), apiKey = (0, env_1.readEnv)('WHOP_API_KEY'), webhookKey = (0, env_1.readEnv)('WHOP_WEBHOOK_SECRET') ?? null, appID = (0, env_1.readEnv)('WHOP_APP_ID') ?? null, userTokenPublicKey = (0, env_1.readEnv)('WHOP_USER_TOKEN_PUBLIC_KEY') ?? null, userTokenJwksUrl = (0, env_1.readEnv)('WHOP_USER_TOKEN_JWKS_URL') ?? null, ...opts } = {}) {
        _Whop_instances.add(this);
        _Whop_encoder.set(this, void 0);
        this.verifyUserToken = (0, verify_user_token_1.makeUserTokenVerifierFromSdk)(this);
        /**
         * Apps
         */
        this.apps = new API.Apps(this);
        /**
         * Invoices
         */
        this.invoices = new API.Invoices(this);
        /**
         * Course lesson interactions
         */
        this.courseLessonInteractions = new API.CourseLessonInteractions(this);
        /**
         * Products
         */
        this.products = new API.Products(this);
        /**
         * Companies
         */
        this.companies = new API.Companies(this);
        /**
         * Webhooks
         */
        this.webhooks = new API.Webhooks(this);
        /**
         * Plans
         */
        this.plans = new API.Plans(this);
        /**
         * Entries
         */
        this.entries = new API.Entries(this);
        /**
         * Forum posts
         */
        this.forumPosts = new API.ForumPosts(this);
        /**
         * Transfers
         */
        this.transfers = new API.Transfers(this);
        /**
         * Ledger accounts
         */
        this.ledgerAccounts = new API.LedgerAccounts(this);
        /**
         * Memberships
         */
        this.memberships = new API.Memberships(this);
        /**
         * Authorized users
         */
        this.authorizedUsers = new API.AuthorizedUsers(this);
        /**
         * App builds
         */
        this.appBuilds = new API.AppBuilds(this);
        /**
         * Shipments
         */
        this.shipments = new API.Shipments(this);
        /**
         * Checkout configurations
         */
        this.checkoutConfigurations = new API.CheckoutConfigurations(this);
        /**
         * Messages
         */
        this.messages = new API.Messages(this);
        /**
         * Chat channels
         */
        this.chatChannels = new API.ChatChannels(this);
        /**
         * Users
         */
        this.users = new API.Users(this);
        /**
         * Payments
         */
        this.payments = new API.Payments(this);
        /**
         * Support channels
         */
        this.supportChannels = new API.SupportChannels(this);
        /**
         * Experiences
         */
        this.experiences = new API.Experiences(this);
        /**
         * Reactions
         */
        this.reactions = new API.Reactions(this);
        /**
         * Members
         */
        this.members = new API.Members(this);
        /**
         * Forums
         */
        this.forums = new API.Forums(this);
        /**
         * Promo codes
         */
        this.promoCodes = new API.PromoCodes(this);
        /**
         * Courses
         */
        this.courses = new API.Courses(this);
        /**
         * Course chapters
         */
        this.courseChapters = new API.CourseChapters(this);
        /**
         * Course lessons
         */
        this.courseLessons = new API.CourseLessons(this);
        /**
         * Reviews
         */
        this.reviews = new API.Reviews(this);
        /**
         * Course students
         */
        this.courseStudents = new API.CourseStudents(this);
        /**
         * Access tokens
         */
        this.accessTokens = new API.AccessTokens(this);
        /**
         * Notifications
         */
        this.notifications = new API.Notifications(this);
        /**
         * Disputes
         */
        this.disputes = new API.Disputes(this);
        /**
         * Refunds
         */
        this.refunds = new API.Refunds(this);
        /**
         * Withdrawals
         */
        this.withdrawals = new API.Withdrawals(this);
        /**
         * Account links
         */
        this.accountLinks = new API.AccountLinks(this);
        this.accounts = new API.Accounts(this);
        this.wallets = new API.Wallets(this);
        this.swaps = new API.Swaps(this);
        this.deposits = new API.Deposits(this);
        /**
         * Setup intents
         */
        this.setupIntents = new API.SetupIntents(this);
        /**
         * Payment methods
         */
        this.paymentMethods = new API.PaymentMethods(this);
        /**
         * Fee markups
         */
        this.feeMarkups = new API.FeeMarkups(this);
        /**
         * Payout methods
         */
        this.payoutMethods = new API.PayoutMethods(this);
        /**
         * Verifications
         */
        this.verifications = new API.Verifications(this);
        /**
         * Leads
         */
        this.leads = new API.Leads(this);
        /**
         * Topups
         */
        this.topups = new API.Topups(this);
        /**
         * Files
         */
        this.files = new API.Files(this);
        /**
         * Company token transactions
         */
        this.companyTokenTransactions = new API.CompanyTokenTransactions(this);
        /**
         * Dm members
         */
        this.dmMembers = new API.DmMembers(this);
        /**
         * Ai chats
         */
        this.aiChats = new API.AIChats(this);
        /**
         * Dm channels
         */
        this.dmChannels = new API.DmChannels(this);
        /**
         * Dispute alerts
         */
        this.disputeAlerts = new API.DisputeAlerts(this);
        /**
         * Resolution center cases
         */
        this.resolutionCenterCases = new API.ResolutionCenterCases(this);
        /**
         * Payout accounts
         */
        this.payoutAccounts = new API.PayoutAccounts(this);
        /**
         * Affiliates
         */
        this.affiliates = new API.Affiliates(this);
        /**
         * Bounties
         */
        this.bounties = new API.Bounties(this);
        /**
         * Ad campaigns
         */
        this.adCampaigns = new API.AdCampaigns(this);
        /**
         * Ad groups
         */
        this.adGroups = new API.AdGroups(this);
        /**
         * Ads
         */
        this.ads = new API.Ads(this);
        /**
         * Conversions
         */
        this.conversions = new API.Conversions(this);
        /**
         * Ad reports
         */
        this.adReports = new API.AdReports(this);
        if (apiKey === undefined) {
            throw new Errors.WhopError("The WHOP_API_KEY environment variable is missing or empty; either provide it, or instantiate the Whop client with an apiKey option, like new Whop({ apiKey: 'My API Key' }).");
        }
        const options = {
            apiKey,
            webhookKey,
            appID,
            userTokenPublicKey,
            userTokenJwksUrl,
            ...opts,
            baseURL: baseURL || `https://api.whop.com/api/v1`,
        };
        this.baseURL = options.baseURL;
        this.timeout = options.timeout ?? _a.DEFAULT_TIMEOUT /* 1 minute */;
        this.logger = options.logger ?? console;
        const defaultLogLevel = 'warn';
        // Set default logLevel early so that we can log a warning in parseLogLevel.
        this.logLevel = defaultLogLevel;
        this.logLevel =
            (0, log_1.parseLogLevel)(options.logLevel, 'ClientOptions.logLevel', this) ??
                (0, log_1.parseLogLevel)((0, env_1.readEnv)('WHOP_LOG'), "process.env['WHOP_LOG']", this) ??
                defaultLogLevel;
        this.fetchOptions = options.fetchOptions;
        this.maxRetries = options.maxRetries ?? 2;
        this.fetch = options.fetch ?? Shims.getDefaultFetch();
        tslib_1.__classPrivateFieldSet(this, _Whop_encoder, Opts.FallbackEncoder, "f");
        const customHeadersEnv = (0, env_1.readEnv)('WHOP_CUSTOM_HEADERS');
        if (customHeadersEnv) {
            const parsed = {};
            for (const line of customHeadersEnv.split('\n')) {
                const colon = line.indexOf(':');
                if (colon >= 0) {
                    parsed[line.substring(0, colon).trim()] = line.substring(colon + 1).trim();
                }
            }
            options.defaultHeaders = { ...parsed, ...options.defaultHeaders };
        }
        this._options = options;
        this.apiKey = apiKey;
        this.webhookKey = webhookKey;
        this.appID = appID;
        this.userTokenPublicKey = userTokenPublicKey;
        this.userTokenJwksUrl = userTokenJwksUrl;
    }
    /**
     * Create a new client instance re-using the same options given to the current client with optional overriding.
     */
    withOptions(options) {
        const client = new this.constructor({
            ...this._options,
            baseURL: this.baseURL,
            maxRetries: this.maxRetries,
            timeout: this.timeout,
            logger: this.logger,
            logLevel: this.logLevel,
            fetch: this.fetch,
            fetchOptions: this.fetchOptions,
            apiKey: this.apiKey,
            webhookKey: this.webhookKey,
            appID: this.appID,
            userTokenPublicKey: this.userTokenPublicKey,
            userTokenJwksUrl: this.userTokenJwksUrl,
            ...options,
        });
        return client;
    }
    defaultQuery() {
        return this._options.defaultQuery;
    }
    validateHeaders({ values, nulls }) {
        return;
    }
    async authHeaders(opts) {
        return (0, headers_1.buildHeaders)([{ Authorization: `Bearer ${this.apiKey}` }]);
    }
    stringifyQuery(query) {
        return (0, query_1.stringifyQuery)(query);
    }
    getUserAgent() {
        return `${this.constructor.name}/JS ${version_1.VERSION}`;
    }
    defaultIdempotencyKey() {
        return `stainless-node-retry-${(0, uuid_1.uuid4)()}`;
    }
    makeStatusError(status, error, message, headers) {
        return Errors.APIError.generate(status, error, message, headers);
    }
    buildURL(path, query, defaultBaseURL) {
        const baseURL = (!tslib_1.__classPrivateFieldGet(this, _Whop_instances, "m", _Whop_baseURLOverridden).call(this) && defaultBaseURL) || this.baseURL;
        const url = (0, values_1.isAbsoluteURL)(path) ?
            new URL(path)
            : new URL(baseURL + (baseURL.endsWith('/') && path.startsWith('/') ? path.slice(1) : path));
        const defaultQuery = this.defaultQuery();
        const pathQuery = Object.fromEntries(url.searchParams);
        if (!(0, values_2.isEmptyObj)(defaultQuery) || !(0, values_2.isEmptyObj)(pathQuery)) {
            query = { ...pathQuery, ...defaultQuery, ...query };
        }
        if (typeof query === 'object' && query && !Array.isArray(query)) {
            url.search = this.stringifyQuery(query);
        }
        return url.toString();
    }
    /**
     * Used as a callback for mutating the given `FinalRequestOptions` object.
     */
    async prepareOptions(options) { }
    /**
     * Used as a callback for mutating the given `RequestInit` object.
     *
     * This is useful for cases where you want to add certain headers based off of
     * the request properties, e.g. `method` or `url`.
     */
    async prepareRequest(request, { url, options }) { }
    get(path, opts) {
        return this.methodRequest('get', path, opts);
    }
    post(path, opts) {
        return this.methodRequest('post', path, opts);
    }
    patch(path, opts) {
        return this.methodRequest('patch', path, opts);
    }
    put(path, opts) {
        return this.methodRequest('put', path, opts);
    }
    delete(path, opts) {
        return this.methodRequest('delete', path, opts);
    }
    methodRequest(method, path, opts) {
        return this.request(Promise.resolve(opts).then((opts) => {
            return { method, path, ...opts };
        }));
    }
    request(options, remainingRetries = null) {
        return new api_promise_1.APIPromise(this, this.makeRequest(options, remainingRetries, undefined));
    }
    async makeRequest(optionsInput, retriesRemaining, retryOfRequestLogID) {
        const options = await optionsInput;
        const maxRetries = options.maxRetries ?? this.maxRetries;
        if (retriesRemaining == null) {
            retriesRemaining = maxRetries;
        }
        await this.prepareOptions(options);
        const { req, url, timeout } = await this.buildRequest(options, {
            retryCount: maxRetries - retriesRemaining,
        });
        await this.prepareRequest(req, { url, options });
        /** Not an API request ID, just for correlating local log entries. */
        const requestLogID = 'log_' + ((Math.random() * (1 << 24)) | 0).toString(16).padStart(6, '0');
        const retryLogStr = retryOfRequestLogID === undefined ? '' : `, retryOf: ${retryOfRequestLogID}`;
        const startTime = Date.now();
        (0, log_1.loggerFor)(this).debug(`[${requestLogID}] sending request`, (0, log_1.formatRequestDetails)({
            retryOfRequestLogID,
            method: options.method,
            url,
            options,
            headers: req.headers,
        }));
        if (options.signal?.aborted) {
            throw new Errors.APIUserAbortError();
        }
        const controller = new AbortController();
        const response = await this.fetchWithTimeout(url, req, timeout, controller).catch(errors_1.castToError);
        const headersTime = Date.now();
        if (response instanceof globalThis.Error) {
            const retryMessage = `retrying, ${retriesRemaining} attempts remaining`;
            if (options.signal?.aborted) {
                throw new Errors.APIUserAbortError();
            }
            // detect native connection timeout errors
            // deno throws "TypeError: error sending request for url (https://example/): client error (Connect): tcp connect error: Operation timed out (os error 60): Operation timed out (os error 60)"
            // undici throws "TypeError: fetch failed" with cause "ConnectTimeoutError: Connect Timeout Error (attempted address: example:443, timeout: 1ms)"
            // others do not provide enough information to distinguish timeouts from other connection errors
            const isTimeout = (0, errors_1.isAbortError)(response) ||
                /timed? ?out/i.test(String(response) + ('cause' in response ? String(response.cause) : ''));
            if (retriesRemaining) {
                (0, log_1.loggerFor)(this).info(`[${requestLogID}] connection ${isTimeout ? 'timed out' : 'failed'} - ${retryMessage}`);
                (0, log_1.loggerFor)(this).debug(`[${requestLogID}] connection ${isTimeout ? 'timed out' : 'failed'} (${retryMessage})`, (0, log_1.formatRequestDetails)({
                    retryOfRequestLogID,
                    url,
                    durationMs: headersTime - startTime,
                    message: response.message,
                }));
                return this.retryRequest(options, retriesRemaining, retryOfRequestLogID ?? requestLogID);
            }
            (0, log_1.loggerFor)(this).info(`[${requestLogID}] connection ${isTimeout ? 'timed out' : 'failed'} - error; no more retries left`);
            (0, log_1.loggerFor)(this).debug(`[${requestLogID}] connection ${isTimeout ? 'timed out' : 'failed'} (error; no more retries left)`, (0, log_1.formatRequestDetails)({
                retryOfRequestLogID,
                url,
                durationMs: headersTime - startTime,
                message: response.message,
            }));
            if (isTimeout) {
                throw new Errors.APIConnectionTimeoutError();
            }
            throw new Errors.APIConnectionError({ cause: response });
        }
        const responseInfo = `[${requestLogID}${retryLogStr}] ${req.method} ${url} ${response.ok ? 'succeeded' : 'failed'} with status ${response.status} in ${headersTime - startTime}ms`;
        if (!response.ok) {
            const shouldRetry = await this.shouldRetry(response);
            if (retriesRemaining && shouldRetry) {
                const retryMessage = `retrying, ${retriesRemaining} attempts remaining`;
                // We don't need the body of this response.
                await Shims.CancelReadableStream(response.body);
                (0, log_1.loggerFor)(this).info(`${responseInfo} - ${retryMessage}`);
                (0, log_1.loggerFor)(this).debug(`[${requestLogID}] response error (${retryMessage})`, (0, log_1.formatRequestDetails)({
                    retryOfRequestLogID,
                    url: response.url,
                    status: response.status,
                    headers: response.headers,
                    durationMs: headersTime - startTime,
                }));
                return this.retryRequest(options, retriesRemaining, retryOfRequestLogID ?? requestLogID, response.headers);
            }
            const retryMessage = shouldRetry ? `error; no more retries left` : `error; not retryable`;
            (0, log_1.loggerFor)(this).info(`${responseInfo} - ${retryMessage}`);
            const errText = await response.text().catch((err) => (0, errors_1.castToError)(err).message);
            const errJSON = (0, values_1.safeJSON)(errText);
            const errMessage = errJSON ? undefined : errText;
            (0, log_1.loggerFor)(this).debug(`[${requestLogID}] response error (${retryMessage})`, (0, log_1.formatRequestDetails)({
                retryOfRequestLogID,
                url: response.url,
                status: response.status,
                headers: response.headers,
                message: errMessage,
                durationMs: Date.now() - startTime,
            }));
            const err = this.makeStatusError(response.status, errJSON, errMessage, response.headers);
            throw err;
        }
        (0, log_1.loggerFor)(this).info(responseInfo);
        (0, log_1.loggerFor)(this).debug(`[${requestLogID}] response start`, (0, log_1.formatRequestDetails)({
            retryOfRequestLogID,
            url: response.url,
            status: response.status,
            headers: response.headers,
            durationMs: headersTime - startTime,
        }));
        return { response, options, controller, requestLogID, retryOfRequestLogID, startTime };
    }
    getAPIList(path, Page, opts) {
        return this.requestAPIList(Page, opts && 'then' in opts ?
            opts.then((opts) => ({ method: 'get', path, ...opts }))
            : { method: 'get', path, ...opts });
    }
    requestAPIList(Page, options) {
        const request = this.makeRequest(options, null, undefined);
        return new Pagination.PagePromise(this, request, Page);
    }
    async fetchWithTimeout(url, init, ms, controller) {
        const { signal, method, ...options } = init || {};
        const abort = this._makeAbort(controller);
        if (signal)
            signal.addEventListener('abort', abort, { once: true });
        const timeout = setTimeout(abort, ms);
        const isReadableBody = (globalThis.ReadableStream && options.body instanceof globalThis.ReadableStream) ||
            (typeof options.body === 'object' && options.body !== null && Symbol.asyncIterator in options.body);
        const fetchOptions = {
            signal: controller.signal,
            ...(isReadableBody ? { duplex: 'half' } : {}),
            method: 'GET',
            ...options,
        };
        if (method) {
            // Custom methods like 'patch' need to be uppercased
            // See https://github.com/nodejs/undici/issues/2294
            fetchOptions.method = method.toUpperCase();
        }
        try {
            // use undefined this binding; fetch errors if bound to something else in browser/cloudflare
            return await this.fetch.call(undefined, url, fetchOptions);
        }
        finally {
            clearTimeout(timeout);
        }
    }
    async shouldRetry(response) {
        // Note this is not a standard header.
        const shouldRetryHeader = response.headers.get('x-should-retry');
        // If the server explicitly says whether or not to retry, obey.
        if (shouldRetryHeader === 'true')
            return true;
        if (shouldRetryHeader === 'false')
            return false;
        // Retry on request timeouts.
        if (response.status === 408)
            return true;
        // Retry on lock timeouts.
        if (response.status === 409)
            return true;
        // Retry on rate limits.
        if (response.status === 429)
            return true;
        // Retry internal errors.
        if (response.status >= 500)
            return true;
        return false;
    }
    async retryRequest(options, retriesRemaining, requestLogID, responseHeaders) {
        let timeoutMillis;
        // Note the `retry-after-ms` header may not be standard, but is a good idea and we'd like proactive support for it.
        const retryAfterMillisHeader = responseHeaders?.get('retry-after-ms');
        if (retryAfterMillisHeader) {
            const timeoutMs = parseFloat(retryAfterMillisHeader);
            if (!Number.isNaN(timeoutMs)) {
                timeoutMillis = timeoutMs;
            }
        }
        // About the Retry-After header: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Retry-After
        const retryAfterHeader = responseHeaders?.get('retry-after');
        if (retryAfterHeader && !timeoutMillis) {
            const timeoutSeconds = parseFloat(retryAfterHeader);
            if (!Number.isNaN(timeoutSeconds)) {
                timeoutMillis = timeoutSeconds * 1000;
            }
            else {
                timeoutMillis = Date.parse(retryAfterHeader) - Date.now();
            }
        }
        // If the API asks us to wait a certain amount of time, just do what it
        // says, but otherwise calculate a default
        if (timeoutMillis === undefined) {
            const maxRetries = options.maxRetries ?? this.maxRetries;
            timeoutMillis = this.calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries);
        }
        await (0, sleep_1.sleep)(timeoutMillis);
        return this.makeRequest(options, retriesRemaining - 1, requestLogID);
    }
    calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries) {
        const initialRetryDelay = 0.5;
        const maxRetryDelay = 8.0;
        const numRetries = maxRetries - retriesRemaining;
        // Apply exponential backoff, but not more than the max.
        const sleepSeconds = Math.min(initialRetryDelay * Math.pow(2, numRetries), maxRetryDelay);
        // Apply some jitter, take up to at most 25 percent of the retry time.
        const jitter = 1 - Math.random() * 0.25;
        return sleepSeconds * jitter * 1000;
    }
    async buildRequest(inputOptions, { retryCount = 0 } = {}) {
        const options = { ...inputOptions };
        const { method, path, query, defaultBaseURL } = options;
        const url = this.buildURL(path, query, defaultBaseURL);
        if ('timeout' in options)
            (0, values_1.validatePositiveInteger)('timeout', options.timeout);
        options.timeout = options.timeout ?? this.timeout;
        const { bodyHeaders, body } = this.buildBody({ options });
        const reqHeaders = await this.buildHeaders({ options: inputOptions, method, bodyHeaders, retryCount });
        const req = {
            method,
            headers: reqHeaders,
            ...(options.signal && { signal: options.signal }),
            ...(globalThis.ReadableStream &&
                body instanceof globalThis.ReadableStream && { duplex: 'half' }),
            ...(body && { body }),
            ...(this.fetchOptions ?? {}),
            ...(options.fetchOptions ?? {}),
        };
        return { req, url, timeout: options.timeout };
    }
    async buildHeaders({ options, method, bodyHeaders, retryCount, }) {
        let idempotencyHeaders = {};
        if (this.idempotencyHeader && method !== 'get') {
            if (!options.idempotencyKey)
                options.idempotencyKey = this.defaultIdempotencyKey();
            idempotencyHeaders[this.idempotencyHeader] = options.idempotencyKey;
        }
        const headers = (0, headers_1.buildHeaders)([
            idempotencyHeaders,
            {
                Accept: 'application/json',
                'User-Agent': this.getUserAgent(),
                'X-Stainless-Retry-Count': String(retryCount),
                ...(options.timeout ? { 'X-Stainless-Timeout': String(Math.trunc(options.timeout / 1000)) } : {}),
                ...(0, detect_platform_1.getPlatformHeaders)(),
                'X-Whop-App-Id': this.appID,
            },
            await this.authHeaders(options),
            this._options.defaultHeaders,
            bodyHeaders,
            options.headers,
        ]);
        this.validateHeaders(headers);
        return headers.values;
    }
    _makeAbort(controller) {
        // note: we can't just inline this method inside `fetchWithTimeout()` because then the closure
        //       would capture all request options, and cause a memory leak.
        return () => controller.abort();
    }
    buildBody({ options: { body, headers: rawHeaders } }) {
        if (!body) {
            return { bodyHeaders: undefined, body: undefined };
        }
        const headers = (0, headers_1.buildHeaders)([rawHeaders]);
        if (
        // Pass raw type verbatim
        ArrayBuffer.isView(body) ||
            body instanceof ArrayBuffer ||
            body instanceof DataView ||
            (typeof body === 'string' &&
                // Preserve legacy string encoding behavior for now
                headers.values.has('content-type')) ||
            // `Blob` is superset of `File`
            (globalThis.Blob && body instanceof globalThis.Blob) ||
            // `FormData` -> `multipart/form-data`
            body instanceof FormData ||
            // `URLSearchParams` -> `application/x-www-form-urlencoded`
            body instanceof URLSearchParams ||
            // Send chunked stream (each chunk has own `length`)
            (globalThis.ReadableStream && body instanceof globalThis.ReadableStream)) {
            return { bodyHeaders: undefined, body: body };
        }
        else if (typeof body === 'object' &&
            (Symbol.asyncIterator in body ||
                (Symbol.iterator in body && 'next' in body && typeof body.next === 'function'))) {
            return { bodyHeaders: undefined, body: Shims.ReadableStreamFrom(body) };
        }
        else if (typeof body === 'object' &&
            headers.values.get('content-type') === 'application/x-www-form-urlencoded') {
            return {
                bodyHeaders: { 'content-type': 'application/x-www-form-urlencoded' },
                body: this.stringifyQuery(body),
            };
        }
        else {
            return tslib_1.__classPrivateFieldGet(this, _Whop_encoder, "f").call(this, { body, headers });
        }
    }
}
exports.Whop = Whop;
_a = Whop, _Whop_encoder = new WeakMap(), _Whop_instances = new WeakSet(), _Whop_baseURLOverridden = function _Whop_baseURLOverridden() {
    return this.baseURL !== 'https://api.whop.com/api/v1';
};
Whop.Whop = _a;
Whop.DEFAULT_TIMEOUT = 60000; // 1 minute
Whop.WhopError = Errors.WhopError;
Whop.APIError = Errors.APIError;
Whop.APIConnectionError = Errors.APIConnectionError;
Whop.APIConnectionTimeoutError = Errors.APIConnectionTimeoutError;
Whop.APIUserAbortError = Errors.APIUserAbortError;
Whop.NotFoundError = Errors.NotFoundError;
Whop.ConflictError = Errors.ConflictError;
Whop.RateLimitError = Errors.RateLimitError;
Whop.BadRequestError = Errors.BadRequestError;
Whop.AuthenticationError = Errors.AuthenticationError;
Whop.InternalServerError = Errors.InternalServerError;
Whop.PermissionDeniedError = Errors.PermissionDeniedError;
Whop.UnprocessableEntityError = Errors.UnprocessableEntityError;
Whop.toFile = Uploads.toFile;
Whop.Apps = apps_1.Apps;
Whop.Invoices = invoices_1.Invoices;
Whop.CourseLessonInteractions = course_lesson_interactions_1.CourseLessonInteractions;
Whop.Products = products_1.Products;
Whop.Companies = companies_1.Companies;
Whop.Webhooks = webhooks_1.Webhooks;
Whop.Plans = plans_1.Plans;
Whop.Entries = entries_1.Entries;
Whop.ForumPosts = forum_posts_1.ForumPosts;
Whop.Transfers = transfers_1.Transfers;
Whop.LedgerAccounts = ledger_accounts_1.LedgerAccounts;
Whop.Memberships = memberships_1.Memberships;
Whop.AuthorizedUsers = authorized_users_1.AuthorizedUsers;
Whop.AppBuilds = app_builds_1.AppBuilds;
Whop.Shipments = shipments_1.Shipments;
Whop.CheckoutConfigurations = checkout_configurations_1.CheckoutConfigurations;
Whop.Messages = messages_1.Messages;
Whop.ChatChannels = chat_channels_1.ChatChannels;
Whop.Users = users_1.Users;
Whop.Payments = payments_1.Payments;
Whop.SupportChannels = support_channels_1.SupportChannels;
Whop.Experiences = experiences_1.Experiences;
Whop.Reactions = reactions_1.Reactions;
Whop.Members = members_1.Members;
Whop.Forums = forums_1.Forums;
Whop.PromoCodes = promo_codes_1.PromoCodes;
Whop.Courses = courses_1.Courses;
Whop.CourseChapters = course_chapters_1.CourseChapters;
Whop.CourseLessons = course_lessons_1.CourseLessons;
Whop.Reviews = reviews_1.Reviews;
Whop.CourseStudents = course_students_1.CourseStudents;
Whop.AccessTokens = access_tokens_1.AccessTokens;
Whop.Notifications = notifications_1.Notifications;
Whop.Disputes = disputes_1.Disputes;
Whop.Refunds = refunds_1.Refunds;
Whop.Withdrawals = withdrawals_1.Withdrawals;
Whop.AccountLinks = account_links_1.AccountLinks;
Whop.Accounts = accounts_1.Accounts;
Whop.Wallets = wallets_1.Wallets;
Whop.Swaps = swaps_1.Swaps;
Whop.Deposits = deposits_1.Deposits;
Whop.SetupIntents = setup_intents_1.SetupIntents;
Whop.PaymentMethods = payment_methods_1.PaymentMethods;
Whop.FeeMarkups = fee_markups_1.FeeMarkups;
Whop.PayoutMethods = payout_methods_1.PayoutMethods;
Whop.Verifications = verifications_1.Verifications;
Whop.Leads = leads_1.Leads;
Whop.Topups = topups_1.Topups;
Whop.Files = files_1.Files;
Whop.CompanyTokenTransactions = company_token_transactions_1.CompanyTokenTransactions;
Whop.DmMembers = dm_members_1.DmMembers;
Whop.AIChats = ai_chats_1.AIChats;
Whop.DmChannels = dm_channels_1.DmChannels;
Whop.DisputeAlerts = dispute_alerts_1.DisputeAlerts;
Whop.ResolutionCenterCases = resolution_center_cases_1.ResolutionCenterCases;
Whop.PayoutAccounts = payout_accounts_1.PayoutAccounts;
Whop.Affiliates = affiliates_1.Affiliates;
Whop.Bounties = bounties_1.Bounties;
Whop.AdCampaigns = ad_campaigns_1.AdCampaigns;
Whop.AdGroups = ad_groups_1.AdGroups;
Whop.Ads = ads_1.Ads;
Whop.Conversions = conversions_1.Conversions;
Whop.AdReports = ad_reports_1.AdReports;
//# sourceMappingURL=client.js.map